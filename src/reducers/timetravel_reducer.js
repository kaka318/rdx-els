import {boxInitialState}  from './boxDown_reducer'
export default (state = boxInitialState,action) => {
  switch(action.type){
    case 'previous':
      return 
    case 'present':
      return
    case 'next':
      return
  }
}